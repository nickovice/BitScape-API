# Test environment configuration
# Development environment configuration
NODE_ENV=test
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres123
DB_DATABASE=test

# For SSL (set to false for local development)
TYPEORM_SSL=false

# Docker Compose Password (should match the one above)
POSTGRES_PASSWORD=postgres123